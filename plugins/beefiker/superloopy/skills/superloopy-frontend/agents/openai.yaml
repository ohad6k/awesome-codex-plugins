interface:
  display_name: "Superloopy Frontend"
  short_description: "Explicit web and Qt desktop GUI workflow"
  default_prompt: "Use Codex `$superloopy:superloopy-frontend` or Claude Code `/superloopy:superloopy-frontend` only for a web frontend or Qt desktop GUI task after explicit invocation or an explicit route from an active `loopy`/`루피` task; do not use it for non-web/non-Qt visual deliverables. Enforce the DESIGN.md token gate and require real-browser rendered-surface evidence for web or native rendered-application evidence for Qt."
